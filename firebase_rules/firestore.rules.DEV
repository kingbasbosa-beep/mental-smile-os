rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEV ONLY (temporary): open read/write
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
